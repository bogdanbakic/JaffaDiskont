<Window x:Class="JaffaDiskont.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:JaffaDiskont.Windows"
        Title="Jaffa" Height="500" Width="400" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Background="#FDBB69" WindowStyle="None">

    <Grid>
        <Button Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0"
                Click="Help_Click" ToolTip="Pomoć" Background="Transparent" BorderThickness="0">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Grid>
                                    <Ellipse x:Name="circle" Fill="Transparent"/>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="circle" Property="Fill" Value="#FFFACD"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
            <TextBlock Text="?" FontSize="18" FontWeight="Bold" Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Button>

        <!-- Logo -->
        <Image Source="/res/logoBez.png" Height="100" VerticalAlignment="Top" Margin="0,10,0,0" HorizontalAlignment="Center"/>

        <!-- Title -->
        <TextBlock Text="Jaffa" FontSize="40" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" Margin="0,115,0,0" VerticalAlignment="Top"/>

        <!-- Custom Tabs Layout -->
        <StackPanel Margin="0,200,0,0" VerticalAlignment="Top">
            <Grid Height="40" Background="#FDBB69">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Content="Uloguj se" Grid.Column="0" Background="#FFE4C4" BorderThickness="0" FontWeight="Bold" Click="ShowLogin"/>
                <Button Content="Registruj se" Grid.Column="1" Background="#FFDAB9" BorderThickness="0" FontWeight="Bold"  Click="ShowRegister"/>
            </Grid>
            <ContentControl x:Name="TabContentPresenter" Width="360" Height="300" Background="White" HorizontalAlignment="Center">
                <local:LoginUC Margin="-20,0,-20,0" />
            </ContentControl>
        </StackPanel>
    </Grid>
</Window>
